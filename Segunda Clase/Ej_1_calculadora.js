const sumar = (num1,num2) => {
    return num1 + num2
}

const multiplicar = (num1,num2) => {
    return num1 * num2
}
// Exportamos solo las funciones especificadas
module.exports = {sumar, multiplicar}